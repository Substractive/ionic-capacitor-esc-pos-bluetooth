import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic Capacitor escpos bluetooth',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
